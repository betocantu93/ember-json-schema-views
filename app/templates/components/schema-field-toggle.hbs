<div class="toggle">
  {{!-- {{x-toggle
    size=toggleSize
    value=value
    showLabels=showLabels
    onToggle=(action "onToggle")
    offLabel=falseLabel
    onLabel=trueLabel
    name=name
    disabled=property.readonly}} --}}
    {{#paper-switch value=value onChange=(action "onToggle") disabled=property.readonly}}
      {{#if value}}
        {{trueLabel}}
      {{else}}
        {{falseLabel}}
      {{/if}}
    {{/paper-switch}} 
</div>

